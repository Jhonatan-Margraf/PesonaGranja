name: suinocultura_ia
description: App de monitoramento de peso de suínos com IA
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- State Management ---
  provider: ^6.1.1

  # --- Hardware & Camera ---
  camera: ^0.11.3+1
  image_picker: ^1.0.7  # Essencial para a Galeria funcionar

  # --- Networking ---
  http: ^1.1.0
  http_parser: ^4.0.2

  # --- Storage & Files ---
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  path: ^1.8.3

  # --- Utilities ---
  uuid: ^4.2.1
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/pig.png